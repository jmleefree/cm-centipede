# Built binary (migrate.sh runs: go build -o main main.go)
/main

# Local SSH keys generated by setup_environment.sh
ssh_keys/

# Local configs filled with real hosts/credentials (templates stay tracked)
config-*.json
!template-config-*.json
